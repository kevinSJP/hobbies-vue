export function updateItemId (state, itemId) {
  state.itemId = itemId
}

export function updateItemName (state, itemName) {
  state.itemName = itemName
}

export function updateScoreType (state, scoreType) {
  state.scoreType = scoreType
}
